import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    height: 58,
    width: '100%',
    borderRadius: 8,
    borderWidth: 1,
    paddingHorizontal: 16,
    fontSize: 24
  },
  errorText: {
    fontSize: 14,
    fontWeight: '400',
    marginTop: 4
  }
});
